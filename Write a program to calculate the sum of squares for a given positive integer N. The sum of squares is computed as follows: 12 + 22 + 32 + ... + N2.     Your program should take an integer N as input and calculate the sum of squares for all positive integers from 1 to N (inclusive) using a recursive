#include <iostream>
using namespace std;

int sumOfSquares(int N) {
    if (N == 1) {
        return 1;
    }
    return N * N + sumOfSquares(N - 1);
}

int main() {
    int N;
    cin >> N;
    int result = sumOfSquares(N);
    cout << result << endl;
    return 0;
}
